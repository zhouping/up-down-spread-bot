# Meridian 交易逻辑

## 系统总览

- **平台**: Polymarket 加密资产涨跌二元期权
- **交易标的**: BTC / ETH / SOL / XRP（共 4 个币种）
- **窗口周期**: 可配置 5 分钟或 15 分钟（config 中 market_window）
- **统一钱包**: 单个 USDC 钱包，所有策略共享
- **策略**: Late Entry V3 — 仅在窗口末尾的最后时间段入场

---

## 一、市场生命周期（每个币种独立）

### 1.1 市场发现
- 市场 slug 格式：`{coin}-updown-{window}-{slot}`，slot = `当前时间戳 / 窗口秒数 * 窗口秒数`
- 每个窗口开始，通过 Gamma API 获取该市场的 UP/DOWN token ID 和 condition_id
- 缓存 token 元数据到 `logs/market_metadata.json`（持久化，重启后可恢复）

### 1.2 入场窗口
- 窗口后半段（config: entry_window_sec，默认 600s）
- 只有 `seconds_till_end ≤ entry_window` 时才尝试入场
- 最后 30 秒：**不进场**（流动性枯竭，填单无意义）

### 1.3 首次市场跳过
- 脚本启动后遇到的**第一个市场标记为 -1（跳过）**，因为启动时市场已运行到中途
- 检测到市场切换后，后续市场正常交易

### 1.4 市场结束
- 当 data_feed 的 market_slug 发生变化 → 旧市场结束
- 旧市场进入赎回流程（由 SimpleRedeemCollector 异步处理）
- DRY_RUN 模式下：直接根据最后 orderbook 状态判断 winner，本地 close 并记录交易
- 早退（stop-loss/flip-stop）结束的市场：走 `close_market_early_exit` 路径

---

## 二、入场逻辑（LateEntryStrategy）

### 2.1 入场条件（全部满足才进场）
| 条件 | 参数 | 默认值 |
|------|------|--------|
| orderbook 数据有效 | asks_stale = false | — |
| 剩余时间 ≤ 入场窗口 | entry_window_sec | 600s |
| 剩余时间 ≥ 30s | — | 30s |
| 距上次入场 ≥ 频率间隔 | entry_frequency_sec | 7s |
| 已入场次数 < 最大次数 | max_entries_per_market | 3 |
| UP+DOWN 价差 ≤ 阀值 | max_spread | 1.05 |
| |UP-DOWN| ≥ 最小置信度 | min_confidence | 0.05 |
| favorite 价格 ≤ 价格上限 | price_max | 0.88 |
| 总投资 ≤ 市场投资上限 | max_investment_per_market / _pct | $300 / 20% |

### 2.2 选择方向
- **favorite** = 出价更高的一边（up_ask > down_ask → 买 UP，否则买 DOWN）
- **只买 favorite 侧**，不做对冲

### 2.3 仓位大小（三档时间梯度）
阈值随窗口长度按比例缩放：
- sizing_t1 = max(15, 180 × scale)  — 15m 约 180s，5m 约 60s
- sizing_t2 = max(10, 120 × scale)  — 15m 约 120s，5m 约 40s

三档仓位：
| 剩余时间 | 固定合约数 | 百分比（钱包余额） |
|---------|-----------|----------------|
| > sizing_t1 | above_180_sec (8) | pct_above_180 (3%) |
| > sizing_t2 | above_120_sec (10) | pct_above_120 (5%) |
| ≤ sizing_t2 | below_120_sec (12) | pct_below_120 (8%) |

### 2.4 全局跨市场资金上限
- 所有市场总投入 ≤ `钱包余额 × 20%`
- 如果本次入场后总投入会超标 → 跳过

---

## 三、订单执行

### 3.1 买入（FAK — Fill And Kill）
- **定价**: ASK 价上浮 5%（激进定价，确保成交）
- **最大尝试**: 3 次 FAK
- **目标成交率**: 98%
- **最小单**: $1.00
- **部分成交跟踪**: 通过 Polymarket API 返回的 takingAmount/makingAmount 获取实际成交数量

### 3.2 卖出（FOK Chunked — 分块填满或取消）
1. **查链上余额**：并行请求多个 RPC 端点，取最快响应
2. **分块**：每块 50 个合约
3. **逐块 FOK 卖出**：以 $0.01 地板价卖出
4. **失败立即重试**：每块最多 3 次
5. **最终扫尾 sweep**：卖完后再次检查余额，重复 FOK
6. **降级兜底**：FOK 失败 → FAK → GTC 市价单
7. **延迟扫尾**：等待 5 秒后再次查余额，捕获 race condition 导致的漏网订单

### 3.3 赎回（on-chain redeem）
- 通过 CTF 合约或 NegRisk Adapter 赎回到期代币
- 先检查 oracle 是否已解析（payoutDenominator > 0）
- Gas price 失败自动重试（最多 5 次，每次提高 20%）
- 赎回后从链上刷新余额

---

## 四、退出逻辑（两路并行）

### 4.1 Stop-Loss（每个币种独立配置）
| 币种 | 启用 | 类型 | 阀值 |
|------|------|------|------|
| BTC | 是 | 百分比 | -50% |
| ETH | 是 | 百分比 | -50% |
| SOL | 是 | 百分比 | -50% |
| XRP | 否 | 固定 | -$11 |

触发条件：`unrealized_pnl ≤ threshold`
- 固定类型：浮亏达到固定美元金额
- 百分比类型：浮亏达到投入本金的百分比

### 4.2 Flip-Stop（价格反转保护）
- **启用**: 是
- **价格阀值**: 0.30
- 如果 favorite 侧价格跌到 0.30 以下 → 立即退出
- 防止价格大幅逆转（买的 favorite 从高处跌到谷底）

### 4.3 退出流程（两者共用）
1. **原子标记**: 在锁内将市场状态设为 -2，确保只触发一次（防多线程竞态）
2. **封锁市场**: 对该币种封锁该市场 slug，阻止新买单进入
3. **卖出所有持仓**: 对 UP 和 DOWN 两侧都执行 FOK Chunked 卖出
4. **计算盈亏**: PnL = favorite_shares × exit_price + non_favorite_shares × (1-exit_price) - total_cost
5. **记录交易**: 先写磁盘，再删内存仓位（防止数据丢失）
6. **滑点分析**: 比较预期卖出价与实际成交价

### 4.4 止损后防再次入场（三重防护）
止损/Flip-Stop 触发后，该市场在当前窗口内 **绝对不可能再次入场**：

1. **`-2` 标志位**: `market_start_prices[coin][slug] = -2`，主循环的入场信号处理和 `on_price_update` 的回调入场逻辑在读取到 `-2` 时直接 `continue` 跳过
2. **按币种封锁**: `_blocked_markets[coin]` 集合，`place_buy_order()` 在每轮 FAK 尝试前都会检查，击中则拒绝下单
3. **仓位已删除**: `close_market_early_exit` 在记录交易后删除 `positions[slug]`，`closed_markets` 集合标记已关闭

只有当前窗口结束、**新 slug 的市场开启**后，才会正常交易新市场。

---

## 五、仓位管理（Trader 类）

### 5.1 仓位结构
- 每个市场独立跟踪 `positions[market_slug]`
- 分 UP/DOWN 两侧记录：总投入、总份额、逐笔记录
- 支持同一市场多次入场（最多 max_entries_per_market 次）

### 5.2 平仓方式
| 方式 | 场景 | PnL 计算 |
|------|------|----------|
| 自然到期 close_market | 窗口结束，winner 已确定 | winner_shares × $1 - total_cost |
| 早退 close_market_early_exit | stop-loss / flip-stop | favorite_shares × exit_price + 另一侧_shares × (1-exit_price) - total_cost |

### 5.3 交易记录持久化
- 每次平仓先写 `trades.jsonl`（含完整盈亏明细）
- 写入成功后再从内存删除仓位
- 重启时自动加载历史 `trades.jsonl` 恢复统计

---

## 六、事件驱动架构

### 6.1 WebSocket 数据流
- 每个币种一个独立 WebSocket 连接到 Polymarket orderbook
- 订阅当前窗口的 UP/DOWN token ID
- 每次 orderbook 变化 → 异步触发 `on_price_update` 回调
- 每个回调在新线程中执行（互不阻塞）

### 6.2 on_price_update 回调（每条价格变化即时处理）
```
价格变化 → 验证价格有效性 → 1) 检查是否退出 → 2) 检查是否入场
```
**第一部分：退出检查**
- 检查价格新鲜度和同步性（up/down 时间戳相差 < 2s，总和 0.95-1.15）
- 检查 stop-loss 条件（根据币种配置的 fixed/percent 类型）
- 检查 flip-stop 条件（favorite 价格 ≤ 0.30）
- 任何条件触发 → 原子标记 → 封锁 → 卖出 → 记录

**第二部分：入场信号**
- 调用 `should_enter()` 检查所有入场条件
- 通过全局资金上限检查
- 再次确认市场未封锁（二次验证竞态条件）
- 执行买入

### 6.3 System #2 — 并行退出扫描
- `ThreadPoolExecutor` 4 线程并行检查所有币种
- 每 0.1s 主循环提交一次检查任务
- 作为事件驱动退出的备份，防止遗漏

---

## 七、赎回系统（SimpleRedeemCollector）

### 7.1 工作模式
- **后台守护线程**，不阻塞主交易循环
- **启动检查**: 启动后 60s，收集重启前未赎回的所有仓位
- **定期检查**: 每 5 分钟扫描 Polymarket redeemable API
- **逐笔赎回**: 串行执行，每笔间隔 1s

### 7.2 赎回流程
1. 查询 API 获取可赎回仓位列表（`redeemable=true`）
2. 从缓存获取 token ID 和 condition_id
3. 调用链上 redeem 交易
4. 赎回成功后通过 API 查询 winner，创建交易记录
5. 发送 Telegram 通知
6. 解除对该市场的资金限制

---

## 八、安全控制

### 8.1 SafetyGuard
- **DRY_RUN 模式**: 完全阻断真实交易
- **单笔最大**: $150
- **每分钟最多**: 100 单
- **每市场总投资上限**: $1000
- **紧急停止**: 出错时激活，阻断所有订单

### 8.2 价格验证（validate_prices）
1. **新鲜度**: UP 和 DOWN 价格更新时间 < 2s
2. **同步性**: UP 和 DOWN 时间戳相差 < 2s
3. **合理性**: UP + DOWN ≈ 1.0（允许范围 0.95-1.15）

### 8.3 按币种开关
- BTC/ETH/SOL: 启用
- XRP: 默认禁用（流动性较低）

---

## 九、Telegram 集成

### 9.1 命令
| 命令 | 功能 |
|------|------|
| /chart /pnl | 生成 PnL 图表 |
| /b /balance | 查看钱包余额（USDC + POL） |
| /t /positions | 查看活跃仓位 |
| /r /redeem | 查看待赎回仓位（含交互按钮）|
| /off /shutdown | 紧急停机 |

### 9.2 自动通知
- 市场关闭通知（含盈亏、胜率、会话统计）
- 市场跳过通知
- 每 N 个已完成市场自动推送 PnL 图表（默认 10 个）

---

## 十、钱包余额追踪

- **启动时**: 从链上查询 USDC 余额（bridged + native）
- **买入时**: 回调扣除
- **卖出/赎回时**: 回调增加
- **定期刷新**: 每个 BTC 市场结束前 60s 从链上刷新
- **链上权威**: 赎回后优先使用链上的精确余额

---

## 十一、回测系统

- 依赖 `record_book_history` 记录的 orderbook 快照文件
- 回放入场逻辑，使用 Gamma API 或历史 trades.jsonl 确定 winner
- 支持参数扫描（grid search）：min_confidence / price_max / entry_window
- 输出：各币种的市场数、胜率、投入、PnL、ROI%
